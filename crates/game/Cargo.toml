[package]
name = "game"
version = "0.1.0"
edition = "2021"

[dependencies]
rapier3d = { version = "0.22.0", features = [ "enhanced-determinism" ] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
rapier3d = { version = "0.22.0", features = [ "enhanced-determinism", "wasm-bindgen" ] }

