[package]
name = "rollback"
version = "0.1.0"
edition = "2024"

[dependencies]
derive_more = { version = "2.0.1", features = ["full"] }
serde = { version = "1.0.216", features = ["derive"] }
macros = { path = "../macros" }
borrow = "1.3.0"

[lib]
path = "src/rollback.rs"
